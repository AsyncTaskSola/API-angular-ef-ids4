@{
    ViewData["Title"] = "About";
}
<h2>@ViewData["Title"]</h2>


<h3>Id Token:
    @ViewData["idtoken"]</h3>
<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dr>@claim.Value</dr>

    }
</dl>